
import { greater } from 'wedding/helpers/greater';
import { module, test } from 'qunit';

module('Unit | Helper | greater');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = greater([42]);
  assert.ok(result);
});

